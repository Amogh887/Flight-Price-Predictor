# Frontend Environment Variables

# The URL of your deployed backend service (e.g., on Render, Railway, Heroku)
# When running locally, this defaults to http://localhost:3001/api
VITE_API_URL=https://your-backend-app.onrender.com/api
